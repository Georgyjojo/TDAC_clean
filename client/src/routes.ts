/** Centralized route targets — single source of truth for navigation. */
export const LOGIN = "/login";
export const HOME = "/";
export const PROJECT = "/project/:projectId";
export const ADMIN_USERS = "/admin/users";