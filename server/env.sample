# Local Termux Postgres (cluster already runs on 127.0.0.1:5432 with
# trust auth). Create the tdacdb database first:
#   psql -h 127.0.0.1 -U postgres -f server/scripts/init_local_db.sql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=tdacdb
DB_USER=postgres
DB_PASSWORD=postgres
