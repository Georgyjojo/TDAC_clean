# TDAC Backend — Python dependencies
# Install with:  pip install -r requirements.txt

# Web framework
fastapi==0.116.1
uvicorn[standard]==0.35.0

# PostgreSQL (async driver used by app/database.py)
asyncpg==0.30.0

# Request / response validation
pydantic==2.11.7

# Excel input-sheet parsing (app/excel_parser.py) + multipart uploads
openpyxl==3.1.5
python-multipart==0.0.32



# JWT handling (app/auth/security.py)
python-jose[cryptography]

# Environment variable loading (app/config.py)
python-dotenv

# Password hashing (app/auth/security.py)
pwdlib[bcrypt]

# --- Testing (server/tests) ---
# Run with:  cd server && ./venv/bin/python -m pytest
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1  # required by fastapi.testclient